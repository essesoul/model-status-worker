{
  "name": "@model-status/worker",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev -c wrangler.template.jsonc --local",
    "build": "wrangler deploy src/index.ts --dry-run --outdir dist --compatibility-date 2026-04-01 --name model-status-worker-dry-run",
    "typecheck": "tsc --noEmit",
    "migrate:local": "wrangler d1 migrations apply DB --local -c wrangler.template.jsonc"
  },
  "dependencies": {
    "@model-status/shared": "0.1.0",
    "hono": "^4.10.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260320.0",
    "typescript": "^5.9.3",
    "wrangler": "^4.42.0"
  }
}
